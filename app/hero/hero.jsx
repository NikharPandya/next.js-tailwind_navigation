import HeroBackground from "./hero_background";

export default function Hero() {
  return (
    <HeroBackground>
      <div>
        <h1>
          This is hero component and this will have three components one for
          background and two for content
        </h1>
      </div>
    </HeroBackground>
  );
}
